import React from 'react';

const Home = ({ darkMode }) => {
  return (
    <div id="home" className={`min-h-screen pt-4 ${darkMode ? 'bg-white' : 'bg-gray-800'}`}>
      <h1 className={`${darkMode?'text-gray-800':'text-white'}`}>Hi, I am</h1>
    </div>
  );
};

export default Home;
